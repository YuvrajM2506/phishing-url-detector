# Base URL of your Python Flask / FastAPI backend
# Default is http://localhost:5000 (Flask) or http://localhost:8000 (FastAPI)
# If backend is offline or not set, the frontend seamlessly uses built-in client heuristics!
VITE_API_BASE_URL=http://localhost:5000
